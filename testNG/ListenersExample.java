package testNG;

import org.testng.ITestListener;                   // Listeners are used to listen the test caser while running and do some action when testcase failed, 
												   // take screenShot at the point etc..

public class ListenersExample implements ITestListener {
	

}

